# AgentFlow 环境配置模板
# ============================
# 复制此文件为 .env.chatbot, .env.qa 等，并填入你的配置

# ============================================================================
# LLM Configuration (核心配置 - 必填)
# ============================================================================

# LLM Provider: openai, azure_openai, vllm, ollama, anthropic, custom
LLM_PROVIDER=openai

# Model name - 填入你的模型名称
LLM_MODEL=your-model-name

# API Configuration - ⚠️ 不要泄露到 git！
LLM_API_KEY=your-api-key-here
LLM_API_BASE=https://your-api-url.com/v1

# Generation Parameters
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
LLM_TOP_P=1.0

# Request Settings
LLM_TIMEOUT=60.0
LLM_MAX_RETRIES=3

# ============================================================================
# Server Configuration (服务器配置 - 如果使用 Gateway)
# ============================================================================

SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# ============================================================================
# Database Configuration (数据库配置 - 可选)
# ============================================================================

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=llm_platform

# ============================================================================
# Redis Configuration (Redis 配置 - 可选)
# ============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================================================
# vLLM Endpoints (本地模型部署 - 可选)
# ============================================================================

VLLM_ENDPOINT_8B=http://localhost:8001/v1
VLLM_ENDPOINT_70B=http://localhost:8002/v1
VLLM_ENDPOINT_MIXTRAL=http://localhost:8003/v1

# ============================================================================
# Logging
# ============================================================================

LOG_LEVEL=info

# ============================================================================
# 使用说明:
# ============================================================================
# 1. 复制此文件并重命名，例如:
#    - .env.chatbot  (对话机器人)
#    - .env.qa       (问答系统)
#    - .env.form     (表单提取)
#
# 2. 填入你的真实配置（特别是 API Key）
#
# 3. 确认 .gitignore 已包含 .env* 规则，避免泄露密钥
#
# 4. 运行时指定配置文件:
#    uv run python examples/chatbot.py --env=.env.chatbot
# ============================================================================

